                          {# {{ include('admin/_form.html.twig', {'button_label': 'Modifier'}) }} #}

{{ form_start(form) }}
    <div class="row items-push">
        <div class="col-lg-3">
            <p class="text-muted">
                Your account’s vital info. Your username will be publicly visible.
            </p>
        </div>
        <div class="col-lg-7 offset-lg-1">
            <div class="mb-4">
                {{ form_label(form.prenom) }}
                {{ form_widget(form.prenom) }}
            </div>
            <div class="mb-4">
                {{ form_label(form.nom) }}
                {{ form_widget(form.nom) }}
            </div>
            <div class="mb-4">
                {{ form_label(form.email) }}
                {{ form_widget(form.email) }}
            </div>
            {# <div class="row mb-4">
                <div class="col-md-10 col-xl-6">
                    <div class="push">
                        <img class="img-avatar" src="{{ asset('assets/images/faces/face1.jpg')}}" alt="">
                    </div>
                    <div class="mb-4">
                        <label class="form-label" for="profile-settings-avatar" class="form-label">Choose new avatar</label>
                        <input class="form-control" type="file" id="profile-settings-avatar" name="profile-settings-avatar">
                    </div>
                </div>
            </div> #}
             <div class="row mb-4">
                <div class="col-md-10 col-xl-6">
                    <div class="push">
                        <img class="img-avatar" style="width: 60%" src="{{ asset('uploads/image/' ~ app.user.img)}}" alt="">
                    </div>
                    <div class="mb-4">
                        {{ form_label(form.img) }}
                        {{ form_widget(form.img) }}
                    </div>
                </div>
            </div>
        </div>
        <div class="text-center"> <!-- Vous pouvez personnaliser la classe CSS selon vos besoins -->
            <button class="btn btn-gradient-primary me-2">{{ button_label|default('Enregistrer') }}</button>
        </div>
    {{ form_end(form) }}
    </div>